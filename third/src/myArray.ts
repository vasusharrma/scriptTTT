const superHeros: string[] = ["Superman", "Ironman", "Thor", "Flash"];

const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean
}


const allUsers: User[] = [];

const newUser: Array<User> = [];

const MLModles: string[][] = [
  ["first model", "second model"],
  ["third model", "fourth model"]
]

allUsers.push({ name: "Vasu Sharma", isActive: true });

superHeros.push("Aquaman");

superHeros.map((hero: string): number => {
  return 12;
})
